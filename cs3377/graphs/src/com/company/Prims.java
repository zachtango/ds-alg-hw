package com.company;

public class Prims {
    /*
        to compute minimum spanning tree
        grow tree in successive stages
        each stage, one node picked as the root, add an edge
            finds a new vertex by choosing edge (u,v) such that
            the cost of (u, v) is the smallest among all edges where
            u is in the tree and v is not
        
     */
}
